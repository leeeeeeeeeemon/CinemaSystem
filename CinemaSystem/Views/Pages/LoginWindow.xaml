<!-- Views/LoginWindow.xaml -->
<Window x:Class="CinemaSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Выбор режима входа"
        Height="480" Width="560"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent">

    <materialDesign:Card UniformCornerRadius="16">
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#1A1A2E" Offset="0"/>
                    <GradientStop Color="#16213E" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,40,0,30">
                <TextBlock Text="🎬" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="STARLIGHT" 
                           FontSize="32" FontWeight="Black"
                           Foreground="#FFEB3B"
                           HorizontalAlignment="Center"
                           />
                <TextBlock Text="Выберите режим входа" 
                           FontSize="15"
                           Foreground="#90A4AE"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Карточки выбора роли -->
            <StackPanel Grid.Row="1" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="0,0,0,0">

                <!-- Клиент -->
                <Button x:Name="ClientButton"
                        Width="180" Height="200"
                        Margin="0,0,16,0"
                        Click="ClientButton_Click"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Padding="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    CornerRadius="12"
                                    Background="#1E2A3A"
                                    BorderBrush="#2A3F55"
                                    BorderThickness="1.5">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="MouseOver">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="border"
                                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                To="#1565C0" Duration="0:0:0.15"/>
                                                <ColorAnimation Storyboard.TargetName="border"
                                                                Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                                To="#42A5F5" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="Pressed"/>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="AccountCircleOutline" 
                                                 Width="56" Height="56"
                                                 Foreground="#42A5F5"
                                                 HorizontalAlignment="Center"
                                                 Margin="0,0,0,16"/>
                        <TextBlock Text="Клиент" 
                                   FontSize="20" FontWeight="SemiBold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="Просмотр сеансов&#x0a;и покупка билетов" 
                                   FontSize="12"
                                   Foreground="#78909C"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Button>

                <!-- Сотрудник -->
                <Button x:Name="EmployeeButton"
                        Width="180" Height="200"
                        Margin="16,0,0,0"
                        Click="EmployeeButton_Click"
                        Padding="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    CornerRadius="12"
                                    Background="#1E2A3A"
                                    BorderBrush="#2A3F55"
                                    BorderThickness="1.5">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="MouseOver">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="border"
                                                                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                To="#4A148C" Duration="0:0:0.15"/>
                                                <ColorAnimation Storyboard.TargetName="border"
                                                                Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                                To="#CE93D8" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="Pressed"/>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <materialDesign:PackIcon Kind="ShieldAccountOutline" 
                                                 Width="56" Height="56"
                                                 Foreground="#CE93D8"
                                                 HorizontalAlignment="Center"
                                                 Margin="0,0,0,16"/>
                        <TextBlock Text="Сотрудник" 
                                   FontSize="20" FontWeight="SemiBold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="Полный доступ&#x0a;к системе" 
                                   FontSize="12"
                                   Foreground="#78909C"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Button>

            </StackPanel>

            <!-- Подвал -->
            <TextBlock Grid.Row="2"
                       Text="© 2026 StarLight Cinema"
                       Foreground="#37474F"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
        </Grid>
    </materialDesign:Card>
</Window>
